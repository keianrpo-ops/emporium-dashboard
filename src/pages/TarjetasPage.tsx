import React from 'react';
import Layout from '../components/Layout';

const TarjetasPage: React.FC = () => {
  return (
    <Layout>
      <h1 className="page-title">Tarjetas</h1>
      <p>Página de control de tarjetas de crédito (límites, cupos, movimientos).</p>
    </Layout>
  );
};

export default TarjetasPage;
